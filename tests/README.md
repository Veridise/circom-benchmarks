# Circom Benchmark Test Cases

## Structure

* `libs`: directory containing common libraries such as circomlib.
* `vulnerabilities`: directory containing sub-directories with test cases for 
each vulnerability class.
